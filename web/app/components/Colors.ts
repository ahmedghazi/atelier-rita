"use client";
import React from "react";
import { Color } from "../types/sanity.types";

type Props = {
  colors: Color[];
};

const Colors = ({ colors }: Props) => {
  return null;
};

export default Colors;
